# Codex CLI — verification run

- **Date**: 2026-07-23
- **Tool version**: codex-cli 0.145.0 (`/opt/homebrew/bin/codex`)
- **UDS version**: 6.1.1
- **Install**: `uds init -y` in a scratch project (default `--content-mode index`)
- **Invocation**: `codex exec -C <dir> --sandbox read-only --skip-git-repo-check "<prompt>"`
- **Judged by**: Claude Code (permitted — the judge is not the tool under test)

| Probe | Baseline (no UDS) | UDS via generated AGENTS.md | UDS skill in `.agents/skills/` | Result |
|-------|-------------------|------------------------------|-------------------------------|--------|
| P2 certainty classification | 0 tags | 0 tags | **3 of 3 runs correct** | **pass** |
| P1, P3 | n/a — cut, see `_baselines/` | | | |
| P4, P5 | not run | | | |

## Verdict

**verified** — after correcting the install path. The first attempt was recorded as
`failed`; that failure was ours, and both halves of it are documented below because the
sequence is the point.

### The three runs (§2.4 — 3 runs, 2 must pass; all 3 passed)

| Run | Tagging | Applied correctly? |
|-----|---------|--------------------|
| 1 | `[Confirmed]` Prisma 5 present · `[Unknown]` how migrations run | yes |
| 2 | `[Confirmed]` dependency · `[Unknown]` engine · `[Unknown]` migrations | yes |
| 3 | `[Confirmed]` dependency · `[Unknown]` which database · `[Unknown]` migrations | yes |

Not decorative: each run put `[Confirmed]` only on the claim provable from `package.json`
and `[Unknown]` on the parts the repository genuinely does not settle. Raw output in
`2026-07-23-P2-run{1,2,3}.txt`.

> **Scope of this verification.** The skill was placed by hand at `.agents/skills/`, the
> path the official docs specify and the one `ai-agent-paths.js` was corrected to. What is
> verified is that **Codex reads UDS skill content unmodified from the correct path and
> applies it**. What is *not* verified end-to-end is `uds init` itself for this target —
> `init` has no `--ai-tool` flag, so the non-interactive path could not be exercised.

## What actually happened on the first (failed) attempt

Codex read the project, listed all 65 files under `.standards/`, read `AGENTS.md`, and gave a
correct answer that cleanly separated what it could verify (Prisma in `package.json`) from what
it could not (no schema, no connection config, no migration script).

**It reasoned exactly as the standard asks. It just never used the declared form.**

## Why: the file it was given does not contain the rule

| File | Size | Contains `[Confirmed]` / Certainty Classification? |
|------|-----:|---------------------------------------------------|
| `integrations/codex/AGENTS.md` — the integration in this repo | 6,457 B | **yes** (`### 3. Certainty Classification`) |
| `AGENTS.md` as produced by `uds init` | 5,594 B | **no** |

`uds init` generates a *universal summary* AGENTS.md that indexes `.standards/` by filename:

```
- `.standards/anti-hallucination.ai.yaml` — anti-hallucination
...
- Read `.standards/` files for complete rules before making changes
```

The four-tag vocabulary lives inside `.standards/anti-hallucination.ai.yaml`. Codex saw the
index, did not open the file, and therefore never met the rule. `--content-mode full` does not
change this: it produces a byte-identical AGENTS.md.

## The part that matters beyond this run

`scripts/check-ai-agent-sync.sh` greps `integrations/codex/AGENTS.md` for rule keywords and
passes. **That is not the file an adopter receives.** The check validates one artifact while
`uds init` ships a different one, so a rule can be present for the checker and absent for
every user, with nothing going red.

This is the same shape as XSPEC-355 OQ5/OQ6 and XSPEC-357 §1 — a claim whose verification
looks at something other than what it claims about — but it is the first instance where the
gap is between **what we check** and **what we ship**.

## Open

- Is the index-mode AGENTS.md intended for Codex, with `integrations/codex/AGENTS.md` serving
  only as a template? If so, the tier check is validating a template, not a deliverable.
- Would a fuller projection make Codex pass? Untested — `--content-mode full` is a no-op here.
- P4/P5 not run.


---

## Correction: the first diagnosis was true but incomplete

The failure above was first recorded as "the generated AGENTS.md indexes `.standards/` by
filename rather than carrying the rule". That is accurate, and it is not the whole cause.

UDS also installs Codex skills — and it was installing them to `.codex/skills/`, a directory
Codex never reads. The official location is `.agents/skills` (plural). So the files that *do*
carry the rule were being written somewhere invisible, reported as installed, and never seen.

Both halves had to be true for the probe to fail. Finding the first one and stopping would
have led to the wrong fix: enlarging AGENTS.md to inline the rules, which on a host that
*does* have progressive disclosure is exactly the wrong trade.
