/* eslint-disable */
// VIOLATION + SUPPRESSION: the file-wide disable above must be INERT.
// `linterOptions.noInlineConfig` is what makes 15.2 — "a wrong rule is fixed,
// never disabled" — structural rather than remembered: the gate cannot be
// switched off from inside the tree it defends.
export const t = Date.now();
