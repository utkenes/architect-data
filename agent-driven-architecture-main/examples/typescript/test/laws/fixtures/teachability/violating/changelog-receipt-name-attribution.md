# CHANGELOG

**Writing an entry.** Every entry carries `**Migrating to it:**` and
`**Teachability:**`. The newest one's finding is measured against the live tree by
`examples/typescript/test/laws/teachability.ts`.

## spine-2

The second marked revision. Both fractions are correct, the Kotlin sentence is
complete, and every name the TypeScript half owes appears in the entry — inside the
KOTLIN sentence. Under a per-name substring search over the whole note that passed,
because `arm`, `name`, `register` and `view` are named on both sides of the port
boundary. The TypeScript half of a receipt could therefore shrink to a bare number
in silence, which is why each port's receipt is now one literal carrying its own
label.

**Teachability:** measured across both fixture blocks in both ports. The contract is
*not* yet sufficient in either. TypeScript 0/10.

- Kotlin 4/8 — inferred: `register`

Kotlin's declared block contract pins `arm` and `view` on both blocks; the roles a
Kotlin author reads off it are `arm`, `name`, `register` and `view`.

**Migrating to it:** nothing to do.

## spine-1

The first marked revision.

**Teachability:** the census at this release read TypeScript 0/6 and Kotlin 2/4.

**Migrating to it:** nothing to do.
