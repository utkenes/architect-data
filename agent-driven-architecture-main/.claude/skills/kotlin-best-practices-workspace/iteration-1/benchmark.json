{
  "metadata": {
    "skill_name": "kotlin-best-practices",
    "skill_path": "<path/to/skill>",
    "executor_model": "<model-name>",
    "analyzer_model": "<model-name>",
    "timestamp": "2026-06-05T08:00:38Z",
    "evals_run": [
      1,
      2,
      3,
      4
    ],
    "runs_per_configuration": 3
  },
  "runs": [
    {
      "eval_id": 1,
      "configuration": "with_skill",
      "run_number": 1,
      "result": {
        "pass_rate": 1.0,
        "passed": 7,
        "failed": 0,
        "total": 7,
        "time_seconds": 349.0,
        "tokens": 22756,
        "tool_calls": 0,
        "errors": 0
      },
      "expectations": [
        {
          "text": "Stream is exposed as a cold Flow returned from a NON-suspend function (not a `suspend fun` returning Flow), so collection stays lazy and cancellable",
          "passed": true,
          "evidence": "PriceStreamClient.kt:74 `public fun ticks(symbol: Symbol): Flow<PriceTick> = channelFlow {` \u2014 a non-suspend fun returning a cold Flow. KDoc (lines 60-73) states 'Returning a non-suspend function over a cold Flow (rather than a suspend fun) keeps connection setup lazy: nothing happens until a collector subscribes.'"
        },
        {
          "text": "Cancellation is correct: no swallowed CancellationException; clean shutdown happens via scope cancellation; no GlobalScope is used",
          "passed": true,
          "evidence": "PriceStreamClient.kt:114-118 `catch (e: CancellationException) { ... throw e }` is the FIRST catch and rethrows; the broad `catch (e: Throwable)` at 119-120 calls `coroutineContext.ensureActive()` as its first statement. No GlobalScope anywhere (grep clean). Shutdown is via collector/scope cancellation tearing down the SSE session; test `collectorCancellationTearsDownStream` (Test l.160-175) verifies job.cancel() leaks no exception."
        },
        {
          "text": "Retries ONLY transient failures (timeouts/5xx/transport) with capped + jittered exponential backoff; does not retry terminal 4xx; not a bare `while(true)` catch-all with no delay",
          "passed": true,
          "evidence": "PriceModels.kt:123-145 `toPriceStreamError` classifies transport (status==null) + 408/425/429 + 500..599 as retryable, all other 4xx as terminal. backoffFor (l.110-114) = `random.nextDouble(0.0, min(maxDelay, base*2^attempt))` \u2014 capped FULL jitter. The while(true) loop (Client l.109) is NOT a bare catch-all: it rethrows immediately on `!isRetryable || attempt>=maxRetries || deadlineExceeded` (l.125-139) and `delay(backoff)` (l.145) before retrying. Test `terminalErrorIsNotRetriedAndPropagates` asserts 401 => 1 call, no retry."
        },
        {
          "text": "The HttpClient/engine and the dispatcher are INJECTED (constructor params) so the class is unit-testable (and any Clock/Random too)",
          "passed": true,
          "evidence": "PriceStreamClient.kt:45-52 constructor injects `client: HttpClient`, `dispatcher: CoroutineDispatcher = Dispatchers.Default`, `clock: Clock = Clock.System`, AND `random: Random = Random.Default`. `flowOn(dispatcher)` applied as last operator (l.153). Tests substitute a Ktor MockEngine, StandardTestDispatcher, Random(0) and a RetryPolicy."
        },
        {
          "text": "Does NOT return a fake or last-known value on failure that masks the outage (no silent fallback that fakes success)",
          "passed": true,
          "evidence": "PriceStreamClient.kt:127-139 on exhaustion/terminal it `throw PriceStreamError(...)` with comment 'Never substitute a fake/last-known tick here \u2014 that would turn an outage into silent stale data on a trading screen.' Sample (PriceStreamSamples.kt:28) surfaces the typed error via `.catch` to onStreamFailed, not a stale price."
        },
        {
          "text": "Avoids constructing Json per-call and frames the stream incrementally (does not re-split an ever-growing accumulated buffer)",
          "passed": true,
          "evidence": "PriceStreamClient.kt:58 `private val json: Json = Json { isLenient = false }` built once per client with comment explaining a per-frame `Json {}` would discard descriptor cache. Framing is incremental via Ktor SSE plugin `session.incoming.collect { event -> ... event.data }` (l.100-106) \u2014 the plugin delivers discrete ServerSentEvent objects, no accumulated-buffer re-split."
        },
        {
          "text": "Stays at Tier 0 for this app-level task: does NOT impose published-API/ABI/explicitApi/publishing rules that don't apply",
          "passed": true,
          "evidence": "transcript.md 'Tier classification' section: 'Tier 0 (Core) only. I deliberately did not raise Tier 1 (ABI dumps, explicitApi() strict, overload-don't-append)... the skill explicitly says imposing those on an app class is noise.' It writes `public`/explicit return types but does not gate on an ABI dump or treat data-class evolution as a binary-compat hazard. No publishing/Maven Central rules imposed in the deliverable."
        }
      ],
      "notes": []
    },
    {
      "eval_id": 2,
      "configuration": "with_skill",
      "run_number": 1,
      "result": {
        "pass_rate": 1.0,
        "passed": 6,
        "failed": 0,
        "total": 6,
        "time_seconds": 426.1,
        "tokens": 127036,
        "tool_calls": 0,
        "errors": 0
      },
      "expectations": [
        {
          "text": "Public surface uses explicit return/property types throughout (no inferred public return types).",
          "passed": true,
          "evidence": "Every public declaration carries an explicit type, e.g. WeatherClient.kt: 'public suspend fun currentForecast(location: Location): Forecast', 'public fun forecastUpdates(location: Location): Flow<Forecast>', 'public fun create(...): WeatherClient', 'public val latitude: Double'. WeatherError.kt: 'public abstract val isRetryable: Boolean'. Transcript reports compiling under '-Xexplicit-api=strict ... exit 0, zero warnings' which mechanically rejects inferred public return types."
        },
        {
          "text": "Errors modeled as a sealed hierarchy (not a growable public data class, not a bare RuntimeException/IllegalStateException).",
          "passed": true,
          "evidence": "WeatherError.kt: 'public sealed class WeatherError(message: String, cause: Throwable?) : Exception(message, cause)' with closed subtypes Network/Timeout/Http/Authentication/Decoding/Unexpected, each with internal constructor. Not a data class, not a bare RuntimeException."
        },
        {
          "text": "Forecast result type is NOT a public data class that will grow \u2014 interface, regular class, or sealed \u2014 to preserve binary compatibility.",
          "passed": true,
          "evidence": "Forecast.kt: 'public class Forecast internal constructor(...)' \u2014 a regular class with an internal constructor, explicitly NOT a data class. KDoc: 'Deliberately a regular class with a private constructor, not a public data class ... adding a field changes its synthesized constructor and copy(...) descriptors (a binary break).'"
        },
        {
          "text": "Signatures use read-only collections; construction via factory/builder, not a public constructor whose descriptor moves when config grows.",
          "passed": true,
          "evidence": "No public mutable collections in signatures; the API surface uses scalar/value types and Flow<Forecast> (no exposed MutableList etc.). Construction is via factory/builder throughout: WeatherClient via 'create(apiKey, configure: WeatherClientConfig.() -> Unit = {})', Location via 'ofCoordinates(...)'. Config growth point is WeatherClientConfig (internal constructor, public mutable knobs added as new properties) \u2014 create()'s descriptor stays frozen. KDoc: 'new knobs are added as new builder methods, never as new parameters on this factory.'"
        },
        {
          "text": "commonMain stays platform-free: no java.*/android.*/platform.Foundation.* in the public surface.",
          "passed": true,
          "evidence": "Imports across files are kotlinx.coroutines.flow.Flow, kotlinx.serialization.*, kotlin.time.Instant, kotlin.time.Duration, kotlin.concurrent.atomics.* \u2014 zero java.*/android.*/platform.Foundation.*. Transcript: 'commonMain-clean: no java.* / platform.* / System.*. Time is kotlin.time.Instant; the close flag is a kotlin.concurrent atomic, not a JVM synchronized block.'"
        },
        {
          "text": "Surfaces at least one publishing or iOS-interop consideration (api() vs implementation(), single-macOS publish, XCFramework, or SKIE).",
          "passed": true,
          "evidence": "Multiple. Transcript 'Notes for the build wiring': 'Declare kotlinx-coroutines-core and kotlinx-serialization-* as api(), not implementation() \u2014 their types (Flow, @Serializable) are in the public ABI.' Also mentions XCFramework, SKIE ('apply SKIE in the framework-producing module only'), and Central Portal publishing. SKIE referenced in source KDoc too (currentForecast surfaces as Swift async, forecastUpdates as AsyncSequence)."
        }
      ],
      "notes": []
    },
    {
      "eval_id": 3,
      "configuration": "with_skill",
      "run_number": 1,
      "result": {
        "pass_rate": 1.0,
        "passed": 9,
        "failed": 0,
        "total": 9,
        "time_seconds": 117.1,
        "tokens": 90817,
        "tool_calls": 0,
        "errors": 0
      },
      "expectations": [
        {
          "text": "Flags that `runCatching` wraps a suspend call and swallows CancellationException.",
          "passed": true,
          "evidence": "Finding #5: \"`runCatching { ... }` wraps a suspend call \u2014 captures cancellation into `Result.failure` ... wraps suspending I/O ... `runCatching` around a suspend call captures `CancellationException` into `Result.failure` \u2014 so a cancelled `latest()` looks like a failed network call instead of propagating. The skill names this exactly ('Never wrap a suspend call in stdlib `runCatching`').\""
        },
        {
          "text": "Flags the empty `catch (e: Exception) {}` in stream() swallowing cancellation.",
          "passed": true,
          "evidence": "Finding #3: \"`catch (e: Exception)` swallows `CancellationException` \u2014 the watchlist Flow cannot be cancelled ... `CancellationException` is an `Exception`, so this catch eats it and the loop keeps going ... the cooperative-cancellation signal is swallowed and the loop never stops \u2014 a leaked coroutine still polling the gateway.\""
        },
        {
          "text": "Flags constructing `Json {}` per call instead of reusing one.",
          "passed": true,
          "evidence": "Finding #6: \"`Json { ignoreUnknownKeys = true }` constructed inline per call ... a fresh `Json { ... }` builder is allocated on every `latest()` invocation. Each `Json {}` discards the per-class serializer/descriptor cache ... Fix. Hoist one reused `Json` to a top-level/`object val` (or inject it).\""
        },
        {
          "text": "Flags returning cached/0.0 on failure as masking the outage (fake success), and the 0.0 sentinel as dangerous.",
          "passed": true,
          "evidence": "Finding #5: \"Returning a default on failure masks outages. On a real network error this returns the stale cached price, or `0.0` if the symbol was never cached. A `0.0` price silently flowing into a trading watchlist / alerts service is a correctness-and-money bug ... the alerts service could fire on a fake zero. The skill is blunt: 'never return a mock/default on failure (it fakes success).'\""
        },
        {
          "text": "Flags `GlobalScope.launch` in prefetch().",
          "passed": true,
          "evidence": "Finding #4: \"`GlobalScope.launch` in `prefetch()` \u2014 orphaned, uncancellable, untestable work ... `GlobalScope` work has no parent, is never cancelled, and outlives any caller ... This is an explicit Core blocking smell.\""
        },
        {
          "text": "Flags the hardcoded `Dispatchers.IO` as hurting testability.",
          "passed": true,
          "evidence": "Finding #8: \"Hardcoded `Dispatchers.IO` inside the class ... Nondeterministic and untestable \u2014 `runTest`'s virtual time can't drive it, and you can't substitute a test dispatcher. The skill requires injecting the dispatcher with a prod default. Fix. Constructor-inject `private val io: CoroutineDispatcher = Dispatchers.IO`.\""
        },
        {
          "text": "Flags `accumulated += chunk` + re-split as O(n^2) and/or re-emitting old/duplicate lines (connects to the freeze symptom).",
          "passed": true,
          "evidence": "Finding #2: \"`stream()` re-parses the entire accumulated buffer every iteration and re-emits every past price ... `accumulated += chunk` then `for (line in accumulated.split(\"\\n\"))` ... every loop re-splits and re-emits all prices seen since the stream opened ... O(n\u00b2) re-scan + unbounded memory ... `+=` on a `String` accumulator inside the loop is the GC-pressure anti-pattern.\" Explicitly connected to 'the streaming felt off' symptom."
        },
        {
          "text": "Flags the bare `while (true)` with no delay/backoff.",
          "passed": true,
          "evidence": "Finding #1: \"`stream()` busy-loops with no `delay`, hammering the endpoint and starving the dispatcher \u2014 this is the freeze ... `while (true) { http.get(...).bodyAsText(); ... }` with no `delay()` and no backoff ... a polling loop needs a paced `delay()` and, on error, jittered capped backoff \u2014 never a free-running `while(true)`.\""
        },
        {
          "text": "Frames the review at Tier 0 and does NOT demand explicitApi/ABI/publishing.",
          "passed": true,
          "evidence": "Tier classification section: \"Internal app data-layer class ... No published API surface, no `commonMain`/`expect`/`actual`, no publishing block. \u2192 Tier 0 (Core) only. I am deliberately not raising ABI / `explicitApi` / multiplatform / publishing concerns; they don't apply here and would bury the real feedback.\" The review raises zero library/publishing demands."
        }
      ],
      "notes": []
    },
    {
      "eval_id": 4,
      "configuration": "with_skill",
      "run_number": 1,
      "result": {
        "pass_rate": 1.0,
        "passed": 4,
        "failed": 0,
        "total": 4,
        "time_seconds": 95.2,
        "tokens": 98649,
        "tool_calls": 0,
        "errors": 0
      },
      "expectations": [
        {
          "text": "Correctly identifies the code as internal/app-module (Tier 0) and scopes the review to rules that apply.",
          "passed": true,
          "evidence": "Dedicated section 'Tier classification (what I'm holding it to)': 'This is an `internal class` in an Android app's `ui.settings` package... Single target, single module, no `commonMain`, no `expect`/`actual`, no `publishing {}`... So this review is **Tier 0 (Core Kotlin) only**.' Findings header: 'Findings (Tier 0, highest-impact first)'."
        },
        {
          "text": "Does NOT raise published-API / binary-compatibility / ABI / explicitApi / @since / Maven-publishing concerns.",
          "passed": true,
          "evidence": "No such concern raised as a finding. The review explicitly DECLINES them: 'I am deliberately **not** raising: explicitApi() / explicit public return types \u2014 Tier 1, published surfaces only... KMP source-set / Native-dispatcher rules \u2014 Tier 2... Publishing / api() vs implementation() / Dokka \u2014 Tier 3.' These are listed as out-of-scope, not as defects."
        },
        {
          "text": "Does NOT flag the `data class SettingsUiState` as a 'growable public data class' problem.",
          "passed": true,
          "evidence": "Opposite of flagging: 'SettingsUiState being a `data class` is *correct and idiomatic* here; the \"don't grow a public data class\" rule is about cross-module binary compat, which does not exist for an internal UI-state holder.' Also under 'Things that are correct': 'The Tier-1 \"don't grow a public data class\" rule does **not** apply (internal, no cross-module ABI).'"
        },
        {
          "text": "Findings stay proportionate (e.g. suggest injecting Dispatchers.IO) and the verdict is the code is basically solid / commit-ready.",
          "passed": true,
          "evidence": "Finding #1 is exactly 'inject the dispatcher' (Severity: Suggestion). Other findings (write-ordering, missing error handling) are marked Suggestion/Note. Verdict line: 'Solid, idiomatic Android ViewModel. Nothing blocking for a commit.' Bottom line: 'Commit-ready. Idiomatic ViewModel, clean state encapsulation...'"
        }
      ],
      "notes": []
    },
    {
      "eval_id": 1,
      "configuration": "without_skill",
      "run_number": 1,
      "result": {
        "pass_rate": 0.86,
        "passed": 6,
        "failed": 1,
        "total": 7,
        "time_seconds": 238.6,
        "tokens": 16862,
        "tool_calls": 0,
        "errors": 0
      },
      "expectations": [
        {
          "text": "Stream is exposed as a cold Flow returned from a NON-suspend function (not a `suspend fun` returning Flow), so collection stays lazy and cancellable",
          "passed": true,
          "evidence": "PriceWatchClient.kt:129-148 `public fun stream(symbol: String, onState): Flow<PriceTick>` \u2014 non-suspend, returns a cold `flow{}` (private `ticks`, l.151-163) wrapped in `.retryWhen{}`. KDoc l.104-106 states 'stream returns a cold Flow: nothing connects until a collector starts.'"
        },
        {
          "text": "Cancellation is correct: no swallowed CancellationException; clean shutdown happens via scope cancellation; no GlobalScope is used",
          "passed": true,
          "evidence": "PriceWatchClient.kt:194-196 `is CancellationException -> false` is the FIRST arm of `isTransient()`, so retryWhen's predicate returns false for cancellation and rethrows it (never retried/swallowed). No GlobalScope (grep clean). ViewModel collects with `launchIn(scope)` (PriceWatchViewModel.kt:38) so scope cancellation tears the stream down."
        },
        {
          "text": "Retries ONLY transient failures (timeouts/5xx/transport) with capped + jittered exponential backoff; does not retry terminal 4xx; not a bare `while(true)` catch-all with no delay",
          "passed": true,
          "evidence": "PriceWatchClient.kt:137-147 uses `Flow.retryWhen` whose predicate calls `isTransient()` (l.194-215): 5xx (ServerResponseException / SSEClientException w/ 5xx status), HttpRequestTimeoutException, IOException => retry; 4xx (ClientRequestException, non-5xx SSEClientException) and SerializationException => give up. Backoff (ExponentialBackoff l.86-96) = `random.nextDouble() * min(base*2^(n-1), maxDelay)` \u2014 capped full jitter via injected delay. Test `does not retry permanent 4xx` asserts 404 => 1 call. No while(true). (Minor: 408/429 are treated as terminal since isTransientStatus is only 500..599, but the assertion's named transient cases \u2014 timeouts/5xx/transport \u2014 are all retried and 4xx is not retried.)"
        },
        {
          "text": "The HttpClient/engine and the dispatcher are INJECTED (constructor params) so the class is unit-testable (and any Clock/Random too)",
          "passed": false,
          "evidence": "PriceWatchClient.kt:116-122 constructor injects `httpClient`, `decoder`, `backoff`, `maxRetries` \u2014 but NO CoroutineDispatcher param. grep for 'dispatcher|flowOn|Dispatchers' across all without_skill outputs returns ZERO hits. The class never sets a dispatcher (no flowOn), so it cannot be pinned/swapped for testing on a TestDispatcher; tests rely solely on runTest's ambient scheduler. The assertion explicitly requires the dispatcher to be an injected constructor param, which it is not. (Random IS injectable via ExponentialBackoff; HttpClient is injected \u2014 but the dispatcher requirement is unmet.)"
        },
        {
          "text": "Does NOT return a fake or last-known value on failure that masks the outage (no silent fallback that fakes success)",
          "passed": true,
          "evidence": "PriceWatchClient.kt:140 retryWhen returns `false` on terminal/exhausted failure => rethrows to the collector; no fallback tick is emitted. ViewModel (PriceWatchViewModel.kt:37) routes the failure to `_error` StateFlow rather than synthesizing a price. No last-known/default value substituted."
        },
        {
          "text": "Avoids constructing Json per-call and frames the stream incrementally (does not re-split an ever-growing accumulated buffer)",
          "passed": true,
          "evidence": "PriceWatchClient.kt:176-178 `JsonPriceTickDecoder(private val json: Json = Json { ignoreUnknownKeys = true })` \u2014 the Json is a field built once per decoder instance, not constructed per decode() call. Framing is incremental via the Ktor SSE plugin `incoming.collect { event: ServerSentEvent -> event.data }` (l.158-161); the plugin yields discrete events, no accumulated-buffer re-split."
        },
        {
          "text": "Stays at Tier 0 for this app-level task: does NOT impose published-API/ABI/explicitApi/publishing rules that don't apply",
          "passed": true,
          "evidence": "The deliverable imposes no ABI dump, no explicitApi() gating, no Maven Central/publishing requirements. transcript.md l.93-94 only NOTES 'public/explicitApi modifiers are present so this drops cleanly into a library module... harmless in an app module' \u2014 it volunteers compatibility rather than imposing publishing rules on the task. No binary-compat hazards flagged against the app code."
        }
      ],
      "notes": [
        "No CoroutineDispatcher is injected; the class is testable only via runTest's ambient scheduler, not by swapping a dispatcher."
      ]
    },
    {
      "eval_id": 2,
      "configuration": "without_skill",
      "run_number": 1,
      "result": {
        "pass_rate": 0.83,
        "passed": 5,
        "failed": 1,
        "total": 6,
        "time_seconds": 180.7,
        "tokens": 82291,
        "tool_calls": 0,
        "errors": 0
      },
      "expectations": [
        {
          "text": "Public surface uses explicit return/property types throughout (no inferred public return types).",
          "passed": true,
          "evidence": "Explicit types throughout: WeatherClient.kt 'public suspend fun forecast(location: Location): WeatherResult', 'public fun create(...): WeatherClient'; Model.kt 'public val fahrenheit: Double get() = ...', 'public fun of(...): Location'; WeatherResult.kt 'public fun WeatherResult.forecastOrNull(): Forecast?'. Transcript: 'Compiled the whole surface under kotlinc -Xexplicit-api=strict ... clean compile, exit 0, no warnings.'"
        },
        {
          "text": "Errors modeled as a sealed hierarchy (not a growable public data class, not a bare RuntimeException/IllegalStateException).",
          "passed": true,
          "evidence": "WeatherError.kt: 'public sealed class WeatherError(message: String, cause: Throwable? = null) : Throwable(message, cause)' with subtypes Network/LocationNotFound/Unauthorized/RateLimited/Server/Unexpected. Sealed hierarchy, not a data class, not a bare RuntimeException."
        },
        {
          "text": "Forecast result type is NOT a public data class that will grow \u2014 interface, regular class, or sealed \u2014 to preserve binary compatibility.",
          "passed": true,
          "evidence": "Model.kt: 'public class Forecast private constructor(...)' with hand-written equals/hashCode/toString and a nested Builder \u2014 explicitly not a data class. KDoc: 'This is intentionally not a data class. In a published library a data class is a binary-compatibility trap.' (The forecast OUTCOME wrapper WeatherResult is additionally a sealed interface.)"
        },
        {
          "text": "Signatures use read-only collections; construction via factory/builder, not a public constructor whose descriptor moves when config grows.",
          "passed": false,
          "evidence": "Configuration.kt exposes a PUBLIC constructor with defaulted params as the documented growth path: 'public class Configuration(public val requestTimeoutMillis: Long = 10_000L, public val maxRetries: Int = 2, public val baseUrl: String = ...)' and KDoc 'New options are added as new defaulted params here plus a new setter on Builder.' Appending a defaulted parameter to a public constructor moves its JVM descriptor (NoSuchMethodError for already-compiled callers) \u2014 exactly the trap this assertion targets. WeatherClient.create() also takes 'configuration: Configuration = Configuration()', and Success/Failure expose public constructors. A Builder exists but does not remove the public-constructor growth path. (Read-only-collections half is vacuously satisfied \u2014 no collections in the surface.)"
        },
        {
          "text": "commonMain stays platform-free: no java.*/android.*/platform.Foundation.* in the public surface.",
          "passed": true,
          "evidence": "No imports of java.*/android.*/platform.Foundation.* in any file; time is modeled as a primitive 'observedAtEpochMillis: Long' rather than a platform type. Transcript: 'no reliance on any JVM-only stdlib (commonMain-safe)' and 'commonMain only. No java.*, no JVM-only types in the public API.'"
        },
        {
          "text": "Surfaces at least one publishing or iOS-interop consideration (api() vs implementation(), single-macOS publish, XCFramework, or SKIE).",
          "passed": true,
          "evidence": "iOS-interop reasoning is explicit in the transcript: 'Consumed from Swift via the Kotlin/Native ObjC bridge ... avoid things that bridge badly: inline value class, and reliance on Kotlin default arguments (Swift can't see them)' and 'sealed (-> Swift switch), suspend (-> Swift async)'. Builders are justified specifically because 'Kotlin defaults don't bridge' to Swift. (Build-dep api()-vs-implementation() and the binary-compatibility-validator are mentioned as 'out of scope', and SKIE/XCFramework are not named.)"
        }
      ],
      "notes": []
    },
    {
      "eval_id": 3,
      "configuration": "without_skill",
      "run_number": 1,
      "result": {
        "pass_rate": 0.78,
        "passed": 7,
        "failed": 2,
        "total": 9,
        "time_seconds": 88.4,
        "tokens": 74371,
        "tool_calls": 0,
        "errors": 0
      },
      "expectations": [
        {
          "text": "Flags that `runCatching` wraps a suspend call and swallows CancellationException.",
          "passed": false,
          "evidence": "not mentioned. Finding #4 discusses `runCatching` but only that it 'only guards the fetch + parse' and that code after the `isFailure` check is unguarded \u2014 it never identifies that `runCatching` wrapping a suspend call captures/swallows `CancellationException`. The CancellationException discussion (finding #2) is about the `catch (Exception)` in `stream()`, not the `runCatching` in `latest()`."
        },
        {
          "text": "Flags the empty `catch (e: Exception) {}` in stream() swallowing cancellation.",
          "passed": true,
          "evidence": "Finding #2: \"`catch (Exception)` swallows `CancellationException` -> the flow can't be cancelled ('prices freeze') ... `CancellationException` is an `Exception`. When the watchlist screen ... is cancelled, the cancellation is delivered by throwing `CancellationException` ... This `catch` eats it and the `while (true)` loop keeps running.\""
        },
        {
          "text": "Flags constructing `Json {}` per call instead of reusing one.",
          "passed": true,
          "evidence": "Finding #8: \"`Json { ignoreUnknownKeys = true }` is allocated on every `latest()` call ... A new `Json` instance is constructed per call inside the hot path. `Json` is designed to be created once and reused. Hoist it to a top-level/companion `private val json`.\""
        },
        {
          "text": "Flags returning cached/0.0 on failure as masking the outage (fake success), and the 0.0 sentinel as dangerous.",
          "passed": true,
          "evidence": "Finding #5: \"`0.0` as a 'no data' sentinel is dangerous for prices ... In a trading app, `0.0` is a real, catastrophic price \u2014 it can trip alerts, show a free instrument, or feed a P&L calc as a 100% loss. A sentinel that collides with a legitimate value is a latent incident.\" Also finding #4 notes the fallback masks a successful-but-malformed response."
        },
        {
          "text": "Flags `GlobalScope.launch` in prefetch().",
          "passed": true,
          "evidence": "Finding #6: \"`GlobalScope.launch` in `prefetch()` \u2014 leaked, uncancellable work ... `GlobalScope` is unbounded and not tied to any lifecycle ... can't be cancelled when the screen/repo is torn down ... `GlobalScope` should essentially never appear in app/library code.\""
        },
        {
          "text": "Flags the hardcoded `Dispatchers.IO` as hurting testability.",
          "passed": false,
          "evidence": "not mentioned as a testability/injection problem. Finding #10 only flags that `stream()` lacks `.flowOn(Dispatchers.IO)`, and it actually praises `latest()`: \"`latest()` correctly wraps its IO in `withContext(Dispatchers.IO)`.\" There is no recommendation to inject the dispatcher and no mention that hardcoding it defeats `runTest`/test substitution."
        },
        {
          "text": "Flags `accumulated += chunk` + re-split as O(n^2) and/or re-emitting old/duplicate lines (connects to the freeze symptom).",
          "passed": true,
          "evidence": "Finding #1: \"`accumulated` is never cleared. It grows without bound (a memory leak), and every iteration re-splits the whole buffer. Every `price:` line ever received is re-emitted on every iteration. Poll 1 emits prices `[a]`. Poll 2 ... re-emits `[a, b]` ... This is exactly 'the streaming felt off' \u2014 stale prices keep reappearing.\""
        },
        {
          "text": "Flags the bare `while (true)` with no delay/backoff.",
          "passed": true,
          "evidence": "Finding #3: \"`while (true)` with no `delay` -> busy-loop hot spin (CPU burn, connection storm) ... no `delay` anywhere ... this loop spins as fast as the CPU allows, firing back-to-back HTTP requests. On error ... the `catch` swallows and immediately re-loops with zero backoff \u2014 a tight retry storm. Fix: add `delay(...)` between polls and a backoff on failure.\""
        },
        {
          "text": "Frames the review at Tier 0 and does NOT demand explicitApi/ABI/publishing.",
          "passed": true,
          "evidence": "The review never demands explicitApi/ABI/binary-compatibility/publishing \u2014 it stays entirely on core app concerns (coroutines, Flow, serialization, error handling, concurrency). However it does NOT explicitly frame the review as 'Tier 0' (no tier language at all); the restraint is implicit rather than stated. Passed on the operative clause (no inappropriate library demands)."
        }
      ],
      "notes": []
    },
    {
      "eval_id": 4,
      "configuration": "without_skill",
      "run_number": 1,
      "result": {
        "pass_rate": 1.0,
        "passed": 4,
        "failed": 0,
        "total": 4,
        "time_seconds": 67.7,
        "tokens": 72379,
        "tool_calls": 0,
        "errors": 0
      },
      "expectations": [
        {
          "text": "Correctly identifies the code as internal/app-module (Tier 0) and scopes the review to rules that apply.",
          "passed": true,
          "evidence": "Opening 'Context:' line: 'Internal app-module ViewModel backing the Settings screen. Not a published library, no external consumers. This is the right framing \u2014 it means binary-compatibility / public-API concerns... do **not** apply here. I'm reviewing it as ordinary app code.' No explicit 'Tier 0' label is used, but the equivalent scoping is correct and stated up front."
        },
        {
          "text": "Does NOT raise published-API / binary-compatibility / ABI / explicitApi / @since / Maven-publishing concerns.",
          "passed": true,
          "evidence": "No such concern raised as a finding. The review affirmatively excludes them: 'binary-compatibility / public-API concerns (explicit API mode, ABI stability, @PublishedApi, deprecation cycles) do **not** apply here.' All six findings are Tier-0 (dispatcher placement, update{}, race/debounce, load() lifecycle, naming, default-flash) \u2014 none invoke library/ABI rules."
        },
        {
          "text": "Does NOT flag the `data class SettingsUiState` as a 'growable public data class' problem.",
          "passed": true,
          "evidence": "No such concern raised. The data class is listed under 'What's already good': 'data class SettingsUiState with sensible defaults.' It is treated as correct, never flagged as a growable/ABI-fragile public data class."
        },
        {
          "text": "Findings stay proportionate (e.g. suggest injecting Dispatchers.IO) and the verdict is the code is basically solid / commit-ready.",
          "passed": true,
          "evidence": "Finding #2 raises the hardcoded Dispatchers.IO / lack of injection seam (main-safety belongs in the repo); other findings are idiom/nit level (update{}, naming, default-flash). Verdict: 'Solid, idiomatic Android ViewModel... no correctness landmines.' Closing: 'Nothing here blocks a commit. The file is fundamentally sound; these are polish items.'"
        }
      ],
      "notes": []
    }
  ],
  "run_summary": {
    "with_skill": {
      "pass_rate": {
        "mean": 1.0,
        "stddev": 0.0,
        "min": 1.0,
        "max": 1.0
      },
      "time_seconds": {
        "mean": 246.85,
        "stddev": 165.7287,
        "min": 95.2,
        "max": 426.1
      },
      "tokens": {
        "mean": 84814.5,
        "stddev": 44201.5682,
        "min": 22756,
        "max": 127036
      }
    },
    "without_skill": {
      "pass_rate": {
        "mean": 0.8675,
        "stddev": 0.0943,
        "min": 0.78,
        "max": 1.0
      },
      "time_seconds": {
        "mean": 143.85,
        "stddev": 80.0188,
        "min": 67.7,
        "max": 238.6
      },
      "tokens": {
        "mean": 61475.75,
        "stddev": 30049.0136,
        "min": 16862,
        "max": 82291
      }
    },
    "delta": {
      "pass_rate": "+0.13",
      "time_seconds": "+103.0",
      "tokens": "+23339"
    }
  },
  "notes": []
}