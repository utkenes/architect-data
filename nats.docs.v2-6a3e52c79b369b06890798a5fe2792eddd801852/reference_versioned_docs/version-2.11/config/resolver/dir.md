# dir

<Reloadable state="reloadable" note="Handled only as part of the whole `resolver` block; replacement DirAccResolver is never Start()ed." />
The path to storing account JWT files when pushed to the server.


## Types

| Type | Description | Choices |
| :--- | :---------- | :------ |
| `string` | - | - |
