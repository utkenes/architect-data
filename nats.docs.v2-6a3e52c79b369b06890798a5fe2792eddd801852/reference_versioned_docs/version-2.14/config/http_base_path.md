# http_base_path

<Reloadable state="not-reloadable" note="If `--http_base_path` was passed at startup the flag value is re-applied over the config on every reload, so the edit is discarded rather than rejected." />
Base path for monitoring endpoints.


## Types

| Type | Description | Choices |
| :--- | :---------- | :------ |
| `string` | - | - |
