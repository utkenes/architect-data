# default_sentinel

<Reloadable state="reloadable" />
User name applied to connections that authenticate with a sentinel
JWT, which carries no permissions of its own.


## Types

| Type | Description | Choices |
| :--- | :---------- | :------ |
| `string` | - | - |
