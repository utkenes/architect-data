# prev_encryption_key

<Aliases aliases="prev_key, prev_ek" />
<Reloadable state="not-reloadable" />
Previous encryption key, kept so existing filestore data can be
read and re-encrypted after `encryption_key` changes.


## Types

| Type | Description | Choices |
| :--- | :---------- | :------ |
| `string` | - | - |
