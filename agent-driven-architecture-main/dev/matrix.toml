# ═══════════════════════════════════════════════════════════════════════════════════════════════
# AGENT-DRIVEN ARCHITECTURE — READINESS MATRIX (the PRESENT tense)
#
# One row per unit. Status advances ONLY when the named proof exists — fail-closed, enforced by
# dev/matrix.ts. Raw edits are hook-blocked; `validate` additionally catches a row hand-edited to
# claim a status it never earned.
#
#   bun dev/matrix.ts dev/matrix.toml list
#   bun dev/matrix.ts dev/matrix.toml prove <ID> --host <pointer>
#   bun dev/matrix.ts dev/matrix.toml set <ID> ready
#
# THE LADDER — the rungs anyone may set:
#   todo       declared in a manifest, nothing built
#   in_flight  someone is building it
#   blocked    needs a ruling — see the campaign item
#   ready      HOST PROOF exists: it builds and passes its gate on this machine
#
# verified IS NOT A RUNG ANYONE SETS — it is derived, fresh, at query time:
#   bun dev/matrix.ts dev/matrix.toml verified <ID>
# A row derives verified ⟺ it is ready AND every recorded edge (path+token) still resolves in the
# CURRENT tree AND the latest completed ci run on main is green AND that run tested what main
# currently is AND a review pointer is present and shape-valid. Nothing sha-shaped is stored: the
# two values compared are fetched at query time, compared, and discarded — so a force-push or a
# squash-merge that preserves content cannot flip the verdict (the e40b8e9 lesson). Offline the
# run leg reports UNCHECKED, and unchecked is unproven.
#
# What a row STORES for that derivation is only what cannot rot: edges (a test file plus the
# token whose deletion removes the proof — the laws.toml idiom) and a review URL. Run ids and
# commit shas are history: they live in dated notes, depended on by nothing.
#
# CONSUMERS READ THIS FILE. `bun dev/matrix.ts dev/matrix.toml unproven` lists what must be
# treated as absent. A capability stays invisible until its row is earned.
# ═══════════════════════════════════════════════════════════════════════════════════════════════

[[rows]]
id = "VERB-SURFACE"
layer = "foundation"
descriptor = "VerbSpec carries the SDK tool surface — toModelOutput, inputExamples, strict, per-tool context — so a block can declare them without a spine edit"
status = "ready"
host_proof = "test/spine/verb-surface.test.ts 4/4 incl. a new verb declaring the surface with zero spine edits + npm test 43 files/493 PASS @ 2026-08-08"
# 2026-08-07 GATES THE CAMPAIGN. Until this row is at least `ready`, SDK-3/4/5/6 are cosmetics
# 2026-08-07 layered over a six-slot IR. See campaign item SDK-1.
edges = [ { path = "examples/typescript/test/spine/verb-surface.test.ts", token = "a NEW verb declares the surface with ZERO spine edits — G11 for the model half" } ]
review = "https://github.com/torad-labs/agent-driven-architecture/pull/15"
# 2026-08-13 target proof retired by the derivation migration — prose that named a run; the run leg is derived fresh at query time now: CI run 31284161862 @ commit e40b8e9 — TypeScript npm test PASS 1m3s (typecheck + typecheck:wall + eslint + biome + vitest, incl. the laws registry asserting against the book) AND Kotlin ./gradlew check PASS 1m59s, on GitHub-hosted runners (not the author's machine)

[[rows]]
id = "MODEL-PORT"
layer = "foundation"
descriptor = "The cognition seam exposes the SDK result rather than {steps, text} — usage, finishReason, toolCalls, warnings survive the boundary"
status = "ready"
host_proof = "test/spine/turn-outcome.test.ts 4/4 + npm test 41 files/487 tests PASS @ 2026-08-08"
edges = [ { path = "examples/typescript/test/spine/turn-outcome.test.ts", token = "reports token usage instead of discarding it" } ]
review = "https://github.com/torad-labs/agent-driven-architecture/pull/15"
# 2026-08-13 target proof retired by the derivation migration — prose that named a run; the run leg is derived fresh at query time now: CI run 31284161862 @ commit e40b8e9 — TypeScript npm test PASS 1m3s (typecheck + typecheck:wall + eslint + biome + vitest, incl. the laws registry asserting against the book) AND Kotlin ./gradlew check PASS 1m59s, on GitHub-hosted runners (not the author's machine)

[[rows]]
id = "AGENT-LOOP"
layer = "adapter"
descriptor = "ToolLoopAgent holds the declared agent; the tool set is built once, and stopWhen is root-owned"
status = "ready"
host_proof = "test/spine/declared-agent.test.ts 4/4 + npm test 39 files/480 tests PASS @ working tree 2026-08-08"
edges = [ { path = "examples/typescript/test/spine/declared-agent.test.ts", token = "builds the tool table ONCE and reuses the same object across turns" } ]
review = "https://github.com/torad-labs/agent-driven-architecture/pull/15"
# 2026-08-13 target proof retired by the derivation migration — prose that named a run; the run leg is derived fresh at query time now: CI run 31284161862 @ commit e40b8e9 — TypeScript npm test PASS 1m3s (typecheck + typecheck:wall + eslint + biome + vitest, incl. the laws registry asserting against the book) AND Kotlin ./gradlew check PASS 1m59s, on GitHub-hosted runners (not the author's machine)

[[rows]]
id = "STEP-CONTROL"
layer = "adapter"
descriptor = "prepareStep + activeTools express the tier allowlist; context bounds ride prepareStep rather than a closure"
status = "ready"
host_proof = "test/spine/step-control.test.ts 3/3 (1 RED, measured by reverting the wiring) + npm test 40 files/483 tests PASS @ 2026-08-08"
edges = [ { path = "examples/typescript/test/spine/step-control.test.ts", token = "offers ONLY the named subset — an unlisted verb never reaches the model" } ]
review = "https://github.com/torad-labs/agent-driven-architecture/pull/15"
# 2026-08-13 target proof retired by the derivation migration — prose that named a run; the run leg is derived fresh at query time now: CI run 31284161862 @ commit e40b8e9 — TypeScript npm test PASS 1m3s (typecheck + typecheck:wall + eslint + biome + vitest, incl. the laws registry asserting against the book) AND Kotlin ./gradlew check PASS 1m59s, on GitHub-hosted runners (not the author's machine)

[[rows]]
id = "TOOL-LIFECYCLE"
layer = "adapter"
descriptor = "The boundary hooks the tool input lifecycle; the pure body runs ONCE per agent action"
status = "blocked"
host_proof = ""
# block-evidence symptom=the pure tool body runs twice per agent action; loop.ts:13-17 states the price and ratifies it unblocks=an operator ruling: change step granularity (relitigates 14.6) or add a boundary seam returning the produced SealedResult to the tool probe=grep -c 'readonly actions: readonly Action\[\]' examples/typescript/src/spine/pure/step-record.ts result=exit=0 2 

[[rows]]
id = "APPROVAL-SEAM"
layer = "composite"
descriptor = "toolApproval routes into the boundary gate; the different-principal rule and the committed refusal both survive"
status = "blocked"
host_proof = "test/spine/approval.test.ts 3/3 incl. the self-confirm refusal still committed + npm test 46 files/502 PASS @ 2026-08-08; needsApproval is the version-correct API at 6.0.208, toolApproval is v7-only"
# 2026-08-07 BLOCKED pending an operator ruling — the request/confirm gate is a book law and the
# 2026-08-07 SDK approval flow carries no principal identity. See campaign item SDK-6.
# block-evidence symptom=toolApproval cannot express the different-principal rule: the SDK approval flow carries no principal identity, so 'a policy tier may confirm, this run may not' is unrepresentable in it — and both are truthfully Agent unblocks=an operator ruling on whether toolApproval routes INTO the boundary gate (gate stays authoritative) or the gate is replaced (loses the authority distinction and the committed refusal) probe=grep -n 'mayConfirm' examples/typescript/src/app/wire.ts && bun dev/campaigns/ledger.ts dev/campaigns/sdk.toml get SDK-6 result=exit=0 189:  readonly mayConfirm?: (by: Authority) => boolean; 195:    mayConfirm: (sig) => config.mayConfirm?.(sig.authority) ?? true, · SDK-6  [todo]  phase=fork    APPROVAL: toolApproval NOT needsApproval
# block-evidence symptom=declaring needsApproval committed outcome=ok for an action the runtime withheld and never executed (recordsBefore=0 -> recordsAfter=1, verb counter 0) unblocks=the full approval lifecycle: run() accepting prior messages, TurnOutcome surfacing the approval request, and a second generate to resume probe=grep -c needsApproval examples/typescript/src/spine/pure/verb.ts result=exit=0 1 
# 2026-08-09 2026-08-09 was ready, now BLOCKED. The seam it certified was withdrawn under review; the earlier target proof described a capability that fabricated history. Re-earning it requires the lifecycle, not another passthrough.
# 2026-08-13 target proof retired by the derivation migration — prose that named a run; the run leg is derived fresh at query time now: CI run 31284161862 @ commit e40b8e9 — TypeScript npm test PASS 1m3s (typecheck + typecheck:wall + eslint + biome + vitest, incl. the laws registry asserting against the book) AND Kotlin ./gradlew check PASS 1m59s, on GitHub-hosted runners (not the author's machine)

[[rows]]
id = "CANCELLATION"
layer = "composite"
descriptor = "The consumer's cancel deadline aborts the underlying model call — abortSignal and timeout reach the SDK"
status = "ready"
host_proof = "test/spine/cancellation.test.ts 3/3 (2 RED before) + npm test 38 files/476 tests PASS @ working tree 2026-08-08; scope caveat in ledger SDK-7"
edges = [ { path = "examples/typescript/test/spine/cancellation.test.ts", token = "forwards the turn's AbortSignal to the provider call" } ]
review = "https://github.com/torad-labs/agent-driven-architecture/pull/15"
# 2026-08-13 target proof retired by the derivation migration — prose that named a run; the run leg is derived fresh at query time now: CI run 31284161862 @ commit e40b8e9 — TypeScript npm test PASS 1m3s (typecheck + typecheck:wall + eslint + biome + vitest, incl. the laws registry asserting against the book) AND Kotlin ./gradlew check PASS 1m59s, on GitHub-hosted runners (not the author's machine)

[[rows]]
id = "PROVIDER"
layer = "surface"
descriptor = "A real @ai-sdk provider is a declared dependency; the scripted offline path remains the keyless default"
status = "ready"
host_proof = "test/spine/provider.test.ts 2/2 (gateway string + mock on one seam) + npm test 42 files/489 PASS @ 2026-08-08; offline remains the keyless default"
# 2026-08-08 2026-08-07 DESCRIPTOR PRE-JUDGES THE FIX. It reads 'a real @ai-sdk provider is a declared dependency'. SDK-8 was reframed after this row was written: the AI Gateway is the default global provider, so a plain model string needs NO @ai-sdk package at all, and that is likely the better answer for a reference implementation. Earning this row must not be read as requiring a provider dependency.
edges = [ { path = "examples/typescript/test/spine/provider.test.ts", token = "accepts a plain gateway model string" } ]
review = "https://github.com/torad-labs/agent-driven-architecture/pull/15"
# 2026-08-13 target proof retired by the derivation migration — prose that named a run; the run leg is derived fresh at query time now: CI run 31284161862 @ commit e40b8e9 — TypeScript npm test PASS 1m3s (typecheck + typecheck:wall + eslint + biome + vitest, incl. the laws registry asserting against the book) AND Kotlin ./gradlew check PASS 1m59s, on GitHub-hosted runners (not the author's machine)

[[rows]]
id = "TELEMETRY"
layer = "composite"
descriptor = "totalUsage (not usage) and finishReason cross the seam and are observable by a consumer"
status = "ready"
host_proof = "test/spine/turn-outcome.test.ts 4/4 (usage/finishReason/warnings cross the seam) + npm test 41 files/487 PASS @ 2026-08-08; totalUsage-vs-usage inverts at v7 - see ledger SDK-9"
# 2026-08-08 2026-08-07 DESCRIPTOR PRE-JUDGES AN OPEN DECISION. It reads 'totalUsage (not usage)'. That is correct at v6 and INVERTED at v7, where result.totalUsage is deprecated and top-level usage accumulates across all steps with final-step data under finalStep. The row cannot be earned until SDK-0 rules the version; whichever way it goes, the descriptor's parenthetical must be re-read, not trusted.
edges = [ { path = "examples/typescript/test/spine/turn-outcome.test.ts", token = "sums usage across the WHOLE turn, not just the final step" } ]
review = "https://github.com/torad-labs/agent-driven-architecture/pull/15"
# 2026-08-13 target proof retired by the derivation migration — prose that named a run; the run leg is derived fresh at query time now: CI run 31284161862 @ commit e40b8e9 — TypeScript npm test PASS 1m3s (typecheck + typecheck:wall + eslint + biome + vitest, incl. the laws registry asserting against the book) AND Kotlin ./gradlew check PASS 1m59s, on GitHub-hosted runners (not the author's machine)

[[rows]]
id = "CONTEXT-PROVENANCE"
layer = "foundation"
descriptor = "The committed context digest is the text the model actually received - render(context) reaches generateText, not only the boundary and the tool bodies"
status = "ready"
host_proof = "test/spine/context-provenance.test.ts 3/3 (RED on unmodified HEAD: 'expected ticket 4118 looks urgent to contain staged[inbox]: caller says the refund…') + examples/typescript npm test 36 files/470 tests PASS incl. typecheck+wall+eslint+biome @ working tree 2026-08-08"
edges = [ { path = "examples/typescript/test/spine/context-provenance.test.ts", token = "sends the rendered context to the model, not only to the tool bodies" } ]
review = "https://github.com/torad-labs/agent-driven-architecture/pull/15"
# 2026-08-13 target proof retired by the derivation migration — prose that named a run; the run leg is derived fresh at query time now: CI run 31284161862 @ commit e40b8e9 — TypeScript npm test PASS 1m3s (typecheck + typecheck:wall + eslint + biome + vitest, incl. the laws registry asserting against the book) AND Kotlin ./gradlew check PASS 1m59s, on GitHub-hosted runners (not the author's machine)

[[rows]]
id = "PROMPT-BOUNDARY"
layer = "adapter"
descriptor = "Instructions and untrusted staged content occupy structurally different fields, so a Perceived body cannot become an instruction"
status = "ready"
host_proof = "test/spine/prompt-boundary.test.ts 3/3 (2 RED before) + npm test 37 files/473 tests PASS @ working tree 2026-08-08"
edges = [ { path = "examples/typescript/test/spine/prompt-boundary.test.ts", token = "keeps an untrusted Perceived body OUT of the system channel" } ]
review = "https://github.com/torad-labs/agent-driven-architecture/pull/15"
# 2026-08-13 target proof retired by the derivation migration — prose that named a run; the run leg is derived fresh at query time now: CI run 31284161862 @ commit e40b8e9 — TypeScript npm test PASS 1m3s (typecheck + typecheck:wall + eslint + biome + vitest, incl. the laws registry asserting against the book) AND Kotlin ./gradlew check PASS 1m59s, on GitHub-hosted runners (not the author's machine)
