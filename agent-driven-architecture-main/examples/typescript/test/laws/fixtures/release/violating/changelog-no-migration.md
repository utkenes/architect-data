# CHANGELOG

Every heading rule passes here. Only the body is missing, and the body is the reason a
log exists for a tier no registry publishes.

## spine-1

The first marked revision. The tier gained a marker file and the roster pins moved with
it, and this entry describes that change at some length without ever telling a copy at
the previous value what to do about it.
