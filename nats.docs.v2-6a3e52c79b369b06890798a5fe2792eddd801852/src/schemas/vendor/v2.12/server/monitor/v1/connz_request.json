{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://nats.io/schemas/server/monitor/v1/connz_request.json",
  "title": "io.nats.server.monitor.v1.connz_request",
  "description": "Request options for connz monitoring endpoint",
  "type": "object",
  "properties": {
    "acc": {
      "type": "string",
      "description": "Filter by account."
    },
    "auth": {
      "type": "boolean",
      "description": "Username indicates if user names should be included in the results."
    },
    "cid": {
      "type": "integer",
      "description": "Filter for this explicit client connection."
    },
    "filter_subject": {
      "type": "string",
      "description": "Filter by subject interest"
    },
    "limit": {
      "type": "integer",
      "description": "Limit is the maximum number of connections that should be returned by Connz()."
    },
    "mqtt_client": {
      "type": "string",
      "description": "Filter for this explicit client connection based on the MQTT client ID"
    },
    "offset": {
      "type": "integer",
      "description": "Offset is used for pagination. Connz() only returns connections starting at this offset from the global results."
    },
    "sort": {
      "type": "string",
      "description": "Sort indicates how the results will be sorted. Check SortOpt for possible values. Only the sort by connection ID (ByCid) is ascending, all others are descending.",
      "enum": [
        "cid",
        "start",
        "subs",
        "pending",
        "msgs_to",
        "msgs_from",
        "bytes_to",
        "bytes_from",
        "last",
        "idle",
        "uptime",
        "stop",
        "reason",
        "rtt"
      ]
    },
    "state": {
      "type": "string",
      "description": "Filter by connection state.",
      "enum": [
        "open",
        "closed",
        "all"
      ]
    },
    "subscriptions": {
      "type": "boolean",
      "description": "Subscriptions indicates if subscriptions should be included in the results."
    },
    "subscriptions_detail": {
      "type": "boolean",
      "description": "SubscriptionsDetail indicates if subscription details should be included in the results"
    },
    "user": {
      "type": "string",
      "description": "Filter by username."
    }
  }
}
