# port

<Reloadable state="not-reloadable" note="A reload that leaves the value identical (including `port: -1`) is fine; any actual change fails." />
Port the server will listen for incoming leaf node
connections.


## Types

| Type | Description | Choices |
| :--- | :---------- | :------ |
| `integer` | - | - |
