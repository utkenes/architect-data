{
  "//": "The gate's block-tests and allow-tests lint DELIBERATELY BROKEN source. C9 is type-aware, so those files need a program to be typed against — this is it. The root tsconfig EXCLUDES this whole directory, so nothing here reaches `npm run typecheck`.",
  "compilerOptions": {
    "target": "ES2023",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "lib": ["ES2023"],
    "strict": true,
    "noEmit": true
  },
  "include": ["violating/**/*.ts", "compliant/**/*.ts", "suppression/**/*.ts"]
}
