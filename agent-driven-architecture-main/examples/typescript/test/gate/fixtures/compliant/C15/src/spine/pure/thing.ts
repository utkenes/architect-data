// COMPLIANT: a spine file names spine vocabulary and nothing else, so the whole
// tier can be copied into a repository that has entirely different features.
import type { ToolName } from "./ids";
export type Held = ToolName;
