# AI Agent 測試快速指南

## 🎯 一鍵測試指令

### 快速開發驗證（推薦）
```bash
cd cli && npm run test:quick
```

### 發現所有測試
```bash
cd cli && npm run test:discover
```

### 快速單元測試
```bash
cd cli && npm run test:unit
```

### 排除 E2E 測試
```bash
cd cli && npm run test:fast
```

## 📊 測試結構速覽

| 類型 | 文件數 | 測試數 | 執行時間 |
|------|--------|--------|----------|
| 單元測試 | 18 份 | 864 個 | < 3 秒 |
| E2E 測試 | 6 份 | 139 個 | 59+ 分鐘 |

## 🚨 AI Agent 注意事項

- ❌ **不要**在背景執行完整測試套件（會超時）
- ✅ **使用**快速開發命令進行日常驗證
- ⚠️ **E2E 測試**需要用戶手動在終端執行

## 🔍 根據變更選擇測試

### 修改核心邏輯
```bash
npm run test:unit
```

### 修改 CLI 命令
```bash
npm test -- tests/commands/ tests/unit/core/
```

### 修改提示界面
```bash
npm test -- tests/prompts/ tests/commands/init.test.js
```

## 📋 詳細文件

- `tests/README.md` - 完整測試指南
- `scripts/test-discovery.mjs` - 測試發現工具
- `TESTING.md` - 測試開發規範

---
*為 AI Agent 優化的測試執行指南*