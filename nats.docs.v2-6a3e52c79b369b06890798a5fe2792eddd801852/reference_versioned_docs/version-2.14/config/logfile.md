# logfile

<Aliases aliases="log_file" />
<Reloadable state="reloadable" note="Ignored if `-l` or `--log` was passed at startup — the flag value is re-applied over the config on every reload." />
Log file name.


## Types

| Type | Description | Choices |
| :--- | :---------- | :------ |
| `string` | - | - |
