VIOLATING, and appended OUTSIDE the delimited step list on purpose: the two statements below
are what a section-local rewrite leaves behind. One section learned that a Kotlin verb
straddles two Gradle modules; the other still says one folder, and both are in the same file.

**A new verb — domain OR presentation — is 4 appends, 3 files, 1 folder. Uniform.**

Measured by adding a real verb and letting the toolchain name the sites: **4 appends, 3 files,
2 Gradle modules. Zero edits at `:app`.**
