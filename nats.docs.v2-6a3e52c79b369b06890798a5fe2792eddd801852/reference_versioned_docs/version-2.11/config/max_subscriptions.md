# max_subscriptions

<Aliases aliases="max_subs" />
<Reloadable state="not-reloadable" note="This is the per-client limit. The per-account `max_subscriptions` under `accounts` is a different key and does reload." />
Maximum numbers of subscriptions per client and leafnode accounts connection. A value of `0` means unlimited.


## Types

| Type | Description | Choices |
| :--- | :---------- | :------ |
| `string` | - | - |
