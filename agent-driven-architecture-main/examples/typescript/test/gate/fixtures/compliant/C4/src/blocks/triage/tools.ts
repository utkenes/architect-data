// ALLOW-TEST C4, fourth half — the SAME `.js` specifier in the form a block
// actually uses. The extension must make no difference in either direction, or
// the rule is keying on the path again.
export type { Signature } from "@adr/spine/pure/actor.js";
