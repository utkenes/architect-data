# type

<Reloadable state="reloadable" />
Where stapled responses are cached. `local` writes them under the
server's store directory.


## Types

| Type | Description | Choices |
| :--- | :---------- | :------ |
| `string` | - | `none`, `local` |
