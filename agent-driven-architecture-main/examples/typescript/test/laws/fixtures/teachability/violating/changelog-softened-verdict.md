# CHANGELOG

**Writing an entry.** Every entry carries `**Migrating to it:**` and
`**Teachability:**`. The newest one's finding is measured against the live tree by
`examples/typescript/test/laws/teachability.ts`.

## spine-2

The second marked revision. Every NUMBER is right and every owed name is present;
only the READING of them is inverted. That is the cheapest softening there is — the
numbers are tedious to fake and the sentence beside them is free — so the verdict is
derived from the same census as the numbers.

**Teachability:** measured across both fixture blocks in both ports. The contract is
already sufficient in both ports; there is nothing left to do here.

- TypeScript 0/10 — inferred: `arm`, `name`, `register`, `view`
- Kotlin 4/8 — inferred: `register`

**Migrating to it:** nothing to do.

## spine-1

The first marked revision.

**Teachability:** the census at this release read TypeScript 0/6 and Kotlin 2/4.

**Migrating to it:** nothing to do.
