// COMPLIANT: its own folder, and the spine by the package name the block
// declares as a dependency — the live idiom since the workspace wall.
import type { ToolName } from "@adr/spine/pure/ids";
import { priorityOf } from "./slice";
export const peek = priorityOf;
export type Named = ToolName;
