A VIOLATING TypeScript step list: the mandatory install names the two packages an app that
drives the boundary directly happens to call, and leaves out the one the COPIED spine
imports. It reads as a run-time judgement and is false at typecheck.

This step list is for YOUR new repository.

Paths it names:
* `src/spine/`
* `src/spine/package.json`
* `src/spine/tsconfig.json`
* `src/spine/pure/version.ts`
* `src/spine/agent/loop.ts`
* `tsconfig.base.json`
* `src/blocks/console/`
* `src/app/wire.ts`
* `src/app/main.ts`

Commands it runs:
* `npm install`
* `npm test`
* `npm run typecheck`
* `npm run demo`

Mandatory install: `npm i @valibot/to-json-schema valibot`

Walked facts it keeps:
* ../../tsconfig.base.json
* re-derived from the bus
