id: no-runblocking-in-common
valid:
  - |
    package good

    suspend fun work(): Int = 1
invalid:
  - |
    package bad

    import kotlinx.coroutines.runBlocking

    fun work(): Int = runBlocking { 1 }
